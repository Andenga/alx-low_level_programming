#include "main.h"

/**
 * int mul(int a, int b): Multiplies two integers
 * @a: First integer.
 * @b: Second integer.
 * 
 * return: Always zero(Success).
*/

int mul(int a, int b)
{
    int product;

    product = a * b;
    
    return(product);
}